# Cerebro — copy to .env and fill in values

# --- Postgres -----------------------------------------------------------------
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# --- Redis --------------------------------------------------------------------
REDIS_PORT=

# --- RabbitMQ -----------------------------------------------------------------
RABBITMQ_USER=
RABBITMQ_PASSWORD=

# --- Backend ------------------------------------------------------------------
BACKEND_PORT=
FRONTEND_URL=

# --- Frontend -----------------------------------------------------------------
VITE_ARCGIS_API_KEY=
VITE_API_BASE_URL=
VITE_WS_BASE_URL=

# --- Local dev only (not used by docker compose) ------------------------------
# docker compose builds these URLs internally using service hostnames
DATABASE_URL=
REDIS_URL=
RABBITMQ_URL=
