# Grafana datasource provisioning that tells Grafana where Prometheus is running.
# Grafana reads this on startup and auto-wires Prometheus as the default datasource

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
